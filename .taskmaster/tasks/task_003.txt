# Task ID: 3
# Title: 데이터베이스 스키마 설계
# Status: pending
# Dependencies: 1
# Priority: high
# Description: 단어, 사용자 학습 진행 상황, 시험 결과를 저장하기 위한 Firestore 데이터베이스 스키마를 설계하고 구현합니다.
# Details:
Firestore 컬렉션 및 문서 설계:

1. `users` 컬렉션:
```typescript
interface User {
  uid: string;
  email: string;
  displayName?: string;
  currentCycle: number; // 1-5
  currentDay: number; // 사이클에 따라 다름
  preferredLanguagePair: string; // 예: 'korean-english'
  createdAt: Timestamp;
}
```

2. `vocabularies` 컬렉션:
```typescript
interface VocabularyWord {
  id: string;
  korean: string;
  english: string;
  german?: string;
  category?: string;
  difficulty?: number;
}
```

3. `testResults` 컬렉션:
```typescript
interface TestResult {
  userId: string;
  date: Timestamp;
  cycle: number;
  day: number;
  languagePair: string;
  correctCount: number;
  totalCount: number;
  words: {
    wordId: string;
    isCorrect: boolean;
    userAnswer: string;
  }[];
}
```

4. 효율적인 쿼리를 위한 인덱스 생성

# Test Strategy:
테스트 문서를 만들어 스키마를 검증합니다. 컬렉션 간의 관계를 확인합니다. 사용자 진행 상황 및 테스트 결과 검색과 같은 일반적인 작업에 대한 쿼리 성능을 테스트합니다.
